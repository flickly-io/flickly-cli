{
  "name": "flickly-cli-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w flickly",
    "typecheck": "npm run typecheck -w flickly",
    "lint": "npm run lint -w flickly",
    "dev": "npm run dev -w flickly"
  },
  "engines": {
    "node": ">=18"
  }
}
